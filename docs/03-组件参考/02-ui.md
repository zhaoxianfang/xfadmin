# UI 基础组件

> 卡片、按钮、徽章、模态框、选项卡、进度、头像、时间线、轮播等通用界面元素，共 55 个。

<!-- 本文件由 `php tools/gen_component_docs.php` 自动生成，请勿手工编辑组件参数表；
     如需修改请改源码注释后重新生成。章节说明可手工维护。 -->

## 目录

- [`card`](#card) — 卡片容器（header/body/footer + 工具按钮 data-action）
- [`statCard`](#statcard) — 指标卡（大数字 + 趋势 + 图标，对标 metrics/widgets）
- [`alert`](#alert) — 警告提示条（variant/可关闭/dismiss）
- [`badge`](#badge) — 徽章标签（状态/数量标记）
- [`button`](#button) — 按钮（variant/size/loading/icon）
- [`dropdown`](#dropdown) — 下拉菜单（Bootstrap dropdown 封装）
- [`modal`](#modal) — 模态框（居中/尺寸/ajax 加载）
- [`offcanvas`](#offcanvas) — 抽屉面板（侧滑浮层）
- [`tabs`](#tabs) — 选项卡（支持上下/左右布局）
- [`accordion`](#accordion) — 手风琴折叠面板
- [`progress`](#progress) — 进度条（variant/条纹/高度）
- [`spinner`](#spinner) — 加载旋转指示器（border/grow 变体）
- [`pagination`](#pagination) — 分页器（页码/上一页/下一页）
- [`listGroup`](#listgroup) — 列表组（卡片式条目列表）
- [`avatar`](#avatar) — 头像（图片/文字/图标/状态点）
- [`avatarGroup`](#avatargroup) — 头像组（重叠堆叠展示多人）
- [`icon`](#icon) — 图标（Tabler 字体图标封装）
- [`iconSet`](#iconset) — 图标集展示（Tabler 图标网格）
- [`toast`](#toast) — 轻提示（右上角自动消失吐司）
- [`timeline`](#timeline) — 时间线（纵向/横向事件流）
- [`carousel`](#carousel) — 轮播图（多图滑动，object-fit 封面）
- [`breadcrumb`](#breadcrumb) — 面包屑导航
- [`tooltip`](#tooltip) — 文字提示气泡（hover 触发）
- [`popover`](#popover) — 弹出框（hover/click 富内容）
- [`placeholder`](#placeholder) — 占位符块（Bootstrap placeholder 闪烁）
- [`collapse`](#collapse) — 折叠面板（单/多展开）
- [`scrollspy`](#scrollspy) — 滚动监听（导航高亮当前区块）
- [`ratio`](#ratio) — 固定宽高比容器（视频/嵌入比例）
- [`rating`](#rating) — 评分（星星/点赞，只读/可交互）
- [`ribbon`](#ribbon) — 缎带角标（卡片右上角标签）
- [`chip`](#chip) — 筹码标签（可删除的紧凑标签）
- [`stepper`](#stepper) — 步骤条（水平步骤指示器，向导导航）
- [`descriptionList`](#descriptionlist) — 描述列表（术语/定义键值对）
- [`loadingButton`](#loadingbutton) — 加载按钮（点击后显示 spinner）
- [`divider`](#divider) — 分割线（文本/图标居中分割）
- [`kbd`](#kbd) — 键盘按键标记（<kbd> 样式）
- [`media`](#media) — 媒体对象（左图右文列表项）
- [`skeleton`](#skeleton) — 骨架屏占位（加载占位动画）
- [`switch`](#switch) — 开关切换（Toggle，PHP 保留字故类名 Toggle）
- [`codeBlock`](#codeblock) — 代码块（语法高亮 + 复制按钮）
- [`empty`](#empty) — 空状态组件（别名，对应 EmptyState）
- [`toolbar`](#toolbar) — 工具栏容器（左右分栏操作区）
- [`searchBox`](#searchbox) — 搜索框（输入+按钮/图标）
- [`colorPalette`](#colorpalette) — 配色方案展示（色块+HEX 值）
- [`typography`](#typography) — 排版展示（display/标题/文本工具类示例）
- [`utilities`](#utilities) — Bootstrap 工具类展示（间距/弹性/边框等）
- [`videoEmbed`](#videoembed) — 视频嵌入（响应式比例容器）
- [`commandPalette`](#commandpalette) — 命令面板（⌘K 快捷命令）
- [`notificationCenter`](#notificationcenter) — 通知中心（消息抽屉）
- [`dropzoneUpload`](#dropzoneupload) — 拖拽上传区（Dropzone 自定义版）
- [`backToTop`](#backtotop) — 回到顶部悬浮按钮
- [`callout`](#callout) — 强调提示块（左侧色条，variant 语义）
- [`countdown`](#countdown) — 倒计时（数字翻牌到零）
- [`countUp`](#countup) — 数字滚动递增动画
- [`invoicePrintButton`](#invoiceprintbutton) — 发票打印 / 下载按钮 触发 window.print()（配合打印样式仅打印发票区域），复刻 inspinia apps-invoice 的打印按钮

## 本章导读

UI 组件是构建页面的积木，可无限嵌套混用。

### 组合范式

```php
echo XfAdmin::card([
    'title'  => '用户统计',
    'tools'  => ['collapse', 'refresh', 'close'],   // 卡片工具按钮
    'body'   => XfAdmin::row(['cols' => [
        ['width' => 6, 'content' => XfAdmin::statCard([...])],
        ['width' => 6, 'content' => XfAdmin::progress(['value' => 68])],
    ]]),
    'footer' => XfAdmin::button(['label' => '查看详情', 'variant' => 'primary']),
]);
```

### 约定与陷阱

- **文本槽位**（`title`/`label`/`text`）自动转义；**内容槽位**（`body`/`content`/`footer`）原样输出 HTML；
- `variant` 走 `ENUM_VARIANT` 白名单（primary/secondary/success/danger/warning/info/light/dark/link），
  非法值回退默认；
- `size` 仅 `sm` / `lg`，非法值可能回退 `lg`；
- 图标统一用 Tabler（`ti ti-*`），不要使用 `data-lucide`；
- 需要 JS 定位元素时**显式传 `id`**（自动 id 为自增 `xf-N`，不稳定）；
- `Modal` / `Offcanvas` 依赖 Bootstrap JS，确认 `XfAdmin::scripts()` 已输出。


---

### `card`

卡片容器（header/body/footer + 工具按钮 data-action）。

> **类**：`zxf\XfAdmin\Components\UI\Card`
> **文件**：`src/Components/UI/Card.php`（71 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::card([
    'title'    => '卡片标题',
    'subtitle' => '副标题',
    'tools'    => ['collapse', 'refresh', 'close'],   // 或自定义 HTML
    'actions'  => '头部右侧自定义HTML',
    'body'     => '内容（任意组件/HTML）',
    'footer'   => '页脚',
    'padding'  => true,    // false 时 body 无内边距（适合放表格）
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::card([
    'title' => null,
    'subtitle' => null,
    'tools' => [],
    'actions' => null,
    'body' => '',
    'footer' => null,
    'padding' => true,
    'class' => null,
]);
```

</details>

> **渲染骨架**：主要 class `card-header` `justify-content-between` `align-items-center` `d-flex` `card-action` `card-action-item` `card-footer` `card`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | mixed | `null` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `subtitle` | mixed | `null` | 副标题文本；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `tools` | array | `[]` | 卡片工具按钮：`['collapse','refresh','close']` 或自定义 HTML；源码用法：`if ($this->get('title') !== null \|\| $this->get('actions') !== null \|\| $this->get('too…` |
| `actions` | mixed | `null` | 操作区内容（按钮组 / 行操作定义）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `footer` | mixed | `null` | 底部内容（原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `padding` | bool | `true` | 是否保留内边距；源码用法：`$html .= '<div class="' . Html::cls('card-body', ['p-0' => ! $this->get('padding')]) …` |
| `class` | mixed | `null` | 附加到根元素的自定义 class；源码用法：`$html = '<div' . $this->attrs(['class' => Html::cls('card', $this->get('class'))]) . …` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `statCard`

指标卡（大数字 + 趋势 + 图标，对标 metrics/widgets）。

> **类**：`zxf\XfAdmin\Components\UI\StatCard`
> **文件**：`src/Components/UI/StatCard.php`（76 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::statCard([
    'title'   => '总用户',
    'value'   => '12,480',
    'icon'    => 'ti ti-users',
    'variant' => 'primary',
    'trend'   => ['text' => '+12.5%', 'direction' => 'up', 'label' => '较上周'],
    'url'     => '/users',
    'counter' => 12480,   // 传数字启用数字滚动动画
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::statCard([
    'title' => '',
    'value' => '',
    'icon' => null,
    'variant' => 'primary',
    'trend' => null,
    'url' => null,
    'counter' => null,
    'prefix' => '',
    'suffix' => '',
    'width' => null,    // 响应式列宽：null=不包裹；数组=['sm'=>6,'xl'=>3] 生成 col-sm-6 col-xl-3；整数=col-md-N col-xl-N
]);
```

</details>

> **渲染骨架**：主要 class `card-body` `d-flex` `align-items-center` `justify-content-between` `gap-2` `my-2` `py-1` `avatar-sm`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义 |
| `value` | string | `''` | 当前值（表单控件值 / 展示数值）；**文本槽位**：输出前自动 HTML 转义 |
| `icon` | mixed | `null` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `variant` | string | `'primary'` | 可选值：`primary` |
| `trend` | mixed | `null` | 趋势值（正/负）；源码用法：`$trend = $this->get('trend');` |
| `url` | mixed | `null` | 链接地址（自动做安全协议校验）；URL：经协议白名单校验（拦截 `javascript:` 等）；开关：非空 / 真值时启用对应区块 |
| `counter` | mixed | `null` | 为 `null` 时不渲染该区块；输出到 `` 属性 |
| `prefix` | string | `''` | **文本槽位**：输出前自动 HTML 转义 |
| `suffix` | string | `''` | 后缀文本；**文本槽位**：输出前自动 HTML 转义 |
| `width` | mixed | `null` | 响应式列宽：null=不包裹；数组=['sm'=>6,'xl'=>3] 生成 col-sm-6 col-xl-3；整数=col-md-N col-xl-N |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `alert`

警告提示条（variant/可关闭/dismiss）。

> **类**：`zxf\XfAdmin\Components\UI\Alert`
> **文件**：`src/Components/UI/Alert.php`（60 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::alert(['variant' => 'success', 'text' => '操作成功', 'dismissible' => true, 'icon' => 'ti ti-check']);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::alert([
    'variant' => 'primary',
    'text' => '',
    'heading' => null,
    'icon' => null,
    'dismissible' => false,
    'soft' => false,    // bg-*-subtle 柔和风格
]);
```

</details>

> **渲染骨架**：主要 class `alert`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；**文本槽位**：输出前自动 HTML 转义 |
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `heading` | mixed | `null` | **文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `icon` | mixed | `null` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `dismissible` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `soft` | bool | `false` | bg-*-subtle 柔和风格 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `badge`

徽章标签（状态/数量标记）。

> **类**：`zxf\XfAdmin\Components\UI\Badge`
> **文件**：`src/Components/UI/Badge.php`（37 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::badge(['text' => '新', 'variant' => 'danger', 'pill' => true]);
XfAdmin::badge(['text' => '5', 'variant' => 'primary', 'soft' => true, 'icon' => 'ti ti-bell']);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::badge([
    'text' => '',
    'variant' => 'primary',
    'pill' => false,
    'soft' => false,    // bg-*-subtle
    'outline' => false,
    'icon' => null,
]);
```

</details>

> **渲染骨架**：主要 class `badge`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义 |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；**文本槽位**：输出前自动 HTML 转义 |
| `pill` | bool | `false` | 源码用法：`return '<span' . $this->attrs(['class' => Html::cls('badge', $style, ['rounded-pill' …` |
| `soft` | bool | `false` | bg-*-subtle |
| `outline` | bool | `false` | 源码用法：`$style = $this->get('outline')` |
| `icon` | mixed | `null` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `button`

按钮（variant/size/loading/icon）。

> **类**：`zxf\XfAdmin\Components\UI\Button`
> **文件**：`src/Components/UI/Button.php`（92 行）
> **依赖插件**：`ladda`

**用法示例**

```php
XfAdmin::button(['text' => '保存', 'variant' => 'primary', 'type' => 'submit']);
XfAdmin::button(['text' => '删除', 'variant' => 'danger', 'soft' => true, 'icon' => 'ti ti-trash']);
XfAdmin::button(['text' => '打开', 'toggle' => 'modal', 'target' => '#my-modal']);
XfAdmin::button(['text' => '提交', 'ladda' => true]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::button([
    'text' => '',
    'variant' => 'primary',
    'type' => 'button',
    'href' => null,
    'size' => null,    // sm | lg
    'soft' => false,
    'outline' => false,
    'ghost' => false,
    'rounded' => false,    // rounded-pill
    'icon' => null,
    'icon_only' => false,
    'disabled' => false,
    'ladda' => false,
    'toggle' => null,    // modal | offcanvas | collapse | dropdown
    'target' => null,
    'onclick' => null,
]);
```

</details>

> **渲染骨架**：主要 class `ladda-label` `btn`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义 |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`$variant = $this->get('variant');` |
| `type` | string | `'button'` | 类型（各组件语义不同，详见该组件说明）；源码用法：`$attrs['type'] = $this->get('type');` |
| `href` | mixed | `null` | 链接地址（同 url）；URL：经协议白名单校验（拦截 `javascript:` 等）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `size` | mixed | `null` | 可选值：`lg` |
| `soft` | bool | `false` | 柔和（浅色底）样式；开关：非空 / 真值时启用对应区块 |
| `outline` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `ghost` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `rounded` | bool | `false` | rounded-pill |
| `icon` | mixed | `null` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义 |
| `icon_only` | bool | `false` | 源码用法：`'btn-icon' => $this->get('icon_only'),` |
| `disabled` | bool | `false` | 是否禁用；源码用法：`'disabled' => $this->get('disabled') && $this->get('href'),` |
| `ladda` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `toggle` | mixed | `null` | modal \| offcanvas \| collapse \| dropdown；开关：非空 / 真值时启用对应区块 |
| `target` | mixed | `null` | 目标（组件语义不同：链接打开方式 `_blank` / 倒计时目标时间 / 数值目标）；开关：非空 / 真值时启用对应区块 |
| `onclick` | mixed | `null` | 开关：非空 / 真值时启用对应区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `dropdown`

下拉菜单（Bootstrap dropdown 封装）。

> **类**：`zxf\XfAdmin\Components\UI\Dropdown`
> **文件**：`src/Components/UI/Dropdown.php`（104 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::dropdown([
    'text'    => '更多操作',
    'variant' => 'light',
    'items'   => [
        ['text' => '编辑', 'url' => '/edit', 'icon' => 'ti ti-pencil'],
        ['divider' => true],
        ['header' => '危险操作'],
        ['text' => '删除', 'url' => '/del', 'class' => 'text-danger'],
    ],
    'direction' => 'down',    // down | up | start | end
    'align'     => null,      // end => dropdown-menu-end
    'split'     => false,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::dropdown([
    'text' => '',
    'variant' => 'light',
    'size' => null,
    'items' => [],
    'direction' => 'down',
    'align' => null,
    'split' => false,
    'toggle' => null,    // 自定义触发器 HTML（完全接管）
    'menu' => null,    // 自定义菜单内容 HTML
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`divider`、`header`、`icon`、`active`、`disabled`、`url`（默认 `#!`）、`onclick`、`target`、`text`

> **渲染骨架**：主要 class `visually-hidden` `dropdown-divider` `btn` `dropdown-menu` `dropdown-item`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义 |
| `variant` | string | `'light'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`$variant = in_array($this->get('variant'), ['primary', 'secondary', 'success', 'dange…` |
| `size` | mixed | `null` | 可选值：`lg` |
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `direction` | string | `'down'` | 分支取值：`up` / `start` / `end` |
| `align` | mixed | `null` | 对齐方式：start \| center \| end；源码用法：`$menuClass = Html::cls('dropdown-menu', $this->get('align') === 'end' ? 'dropdown-men…` |
| `split` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `toggle` | mixed | `null` | 自定义触发器 HTML（完全接管）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `menu` | mixed | `null` | 自定义菜单内容 HTML；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `modal`

模态框（居中/尺寸/ajax 加载）。

> **类**：`zxf\XfAdmin\Components\UI\Modal`
> **文件**：`src/Components/UI/Modal.php`（73 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::modal([
    'id'      => 'user-modal',
    'title'   => '编辑用户',
    'body'    => $form,
    'footer'  => '<button class="btn btn-light" data-bs-dismiss="modal">取消</button><button class="btn btn-primary">保存</button>',
    'size'    => 'lg',          // sm | lg | xl | fullscreen
    'centered'=> true,
    'scrollable' => false,
    'static'  => false,          // 点击遮罩不关闭
    'trigger' => '打开弹窗',      // 可选：自动生成触发按钮
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::modal([
    'title' => null,
    'body' => '',
    'footer' => null,
    'size' => null,
    'centered' => false,
    'scrollable' => false,
    'fade' => true,
    'static' => false,
    'close' => true,
    'trigger' => null,
    'trigger_variant' => 'primary',
]);
```

</details>

> **渲染骨架**：主要 class `modal-content` `modal-header` `modal-body` `modal-footer` `modal-dialog` `modal`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | mixed | `null` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `footer` | mixed | `null` | 底部内容（原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `size` | mixed | `null` | 枚举白名单 `'sm', 'lg', 'xl', 'fullscreen', 'fullscreen-sm-down', 'fullscreen-md-down', 'fullscreen-lg-down', 'fullscreen-xl-down', 'fullscreen-xxl-down'` |
| `centered` | bool | `false` | 是否垂直居中；源码用法：`'modal-dialog-centered' => $this->get('centered'),` |
| `scrollable` | bool | `false` | 内容超长时是否内部滚动；源码用法：`'modal-dialog-scrollable' => $this->get('scrollable'),` |
| `fade` | bool | `true` | 是否启用淡入动画；源码用法：`'class' => Html::cls('modal', ['fade' => $this->get('fade')]),` |
| `static` | bool | `false` | 点击遮罩不关闭（静态背景）；开关：非空 / 真值时启用对应区块 |
| `close` | bool | `true` | 是否显示关闭按钮；开关：非空 / 真值时启用对应区块 |
| `trigger` | mixed | `null` | 触发方式（hover / click / focus），或触发按钮文案；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `trigger_variant` | string | `'primary'` | 触发按钮的语义变体（primary/secondary/…）；源码用法：`$html .= '<button type="button" class="btn btn-' . $this->enum($this->get('trigger_va…` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `offcanvas`

抽屉面板（侧滑浮层）。

> **类**：`zxf\XfAdmin\Components\UI\Offcanvas`
> **文件**：`src/Components/UI/Offcanvas.php`（54 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::offcanvas([
    'id'        => 'filter-panel',
    'title'     => '筛选',
    'body'      => $form,
    'placement' => 'end',      // start | end | top | bottom
    'backdrop'  => true,
    'trigger'   => '打开筛选',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::offcanvas([
    'title' => null,
    'body' => '',
    'placement' => 'end',
    'backdrop' => true,
    'scroll' => false,
    'trigger' => null,
    'trigger_variant' => 'primary',
]);
```

</details>

> **渲染骨架**：主要 class `offcanvas-header` `offcanvas-body`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | mixed | `null` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义 |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `placement` | string | `'end'` | 可选值：`start` |
| `backdrop` | bool | `true` | 开关：非空 / 真值时启用对应区块 |
| `scroll` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `trigger` | mixed | `null` | 触发方式（hover / click / focus），或触发按钮文案；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `trigger_variant` | string | `'primary'` | 触发按钮的语义变体（primary/secondary/…）；源码用法：`$html .= '<button class="btn btn-' . $this->enum($this->get('trigger_variant'), array…` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `tabs`

选项卡（支持上下/左右布局）。

> **类**：`zxf\XfAdmin\Components\UI\Tabs`
> **文件**：`src/Components/UI/Tabs.php`（97 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::tabs([
    'style' => 'tabs',                 // tabs | pills | underline
    'items' => [
        ['title' => '基本信息', 'icon' => 'ti ti-home', 'content' => '...', 'active' => true],
        ['title' => '安全设置', 'content' => $form],
    ],
    'justified' => false,
    'vertical'  => false,
    'fade'      => true,
    'badge'     => 每个 item 可带 'badge' => '新' 或 ['text' => '谨慎', 'class' => 'bg-warning'],
    'footer'    => '<button ...>提交</button>',   // 整个 tab-content 下方的公共区域（如统一提交按钮）
    'form'      => ['action' => '/save', 'method' => 'POST'],  // 用表单包裹全部面板 + footer（多标签一次提交）
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::tabs([
    'style' => 'tabs',
    'items' => [],
    'justified' => false,
    'vertical' => false,
    'fade' => true,
    'footer' => null,
    'form' => null,
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`id`、`active`、`icon`、`badge`、`disabled`、`title`、`content`

> **渲染骨架**：主要 class `tab-content` `pt-3` `xf-tabs-footer` `border-top` `mt-2` `flex-grow-1` `nav` `nav-link`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `style` | string | `'tabs'` | 附加到根元素的内联样式；**文本槽位**：输出前自动 HTML 转义 |
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `justified` | bool | `false` | 源码用法：`'nav-justified' => $this->get('justified'),` |
| `vertical` | bool | `false` | 是否纵向排列；开关：非空 / 真值时启用对应区块 |
| `fade` | bool | `true` | 是否启用淡入动画；源码用法：`$pane .= '<div class="' . Html::cls('tab-pane', ['fade' => $this->get('fade'), 'show'…` |
| `footer` | mixed | `null` | 底部内容（原样输出）；源码用法：`$footer = $this->get('footer');` |
| `form` | mixed | `null` | 源码用法：`$form = $this->get('form');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `accordion`

手风琴折叠面板。

> **类**：`zxf\XfAdmin\Components\UI\Accordion`
> **文件**：`src/Components/UI/Accordion.php`（46 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::accordion([
    'items' => [
        ['title' => '第一项', 'content' => '...', 'open' => true],
        ['title' => '第二项', 'content' => '...'],
    ],
    'flush'       => false,
    'always_open' => false,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::accordion([
    'items' => [],
    'flush' => false,
    'always_open' => false,
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`id`、`open`、`title`、`content`

> **渲染骨架**：主要 class `accordion-item` `accordion-body` `accordion` `accordion-button` `accordion-collapse` `collapse`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `flush` | bool | `false` | 是否无边框（list-group-flush）；源码用法：`$html = '<div' . $this->attrs(['class' => Html::cls('accordion', ['accordion-flush' =…` |
| `always_open` | bool | `false` | 源码用法：`. ($this->get('always_open') ? '' : ' data-bs-parent="#' . $this->e($id) . '"') . '>'` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `progress`

进度条（variant/条纹/高度）。

> **类**：`zxf\XfAdmin\Components\UI\Progress`
> **文件**：`src/Components/UI/Progress.php`（62 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::progress(['value' => 60, 'variant' => 'success', 'striped' => true, 'animated' => true, 'label' => '60%']);
XfAdmin::progress(['bars' => [['value' => 30, 'variant' => 'success'], ['value' => 20, 'variant' => 'warning']]]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::progress([
    'value' => 0,
    'variant' => 'primary',
    'height' => null,    // px
    'striped' => false,
    'animated' => false,
    'label' => null,
    'bars' => [],
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `bars[]` 元素键：`value`、`variant`（默认 `primary`）、`striped`、`animated`、`label`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `value` | int | `0` | 当前值（表单控件值 / 展示数值）；输出到 `` 属性 |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`'variant' => $this->get('variant'),` |
| `height` | mixed | `null` | px；为 `null` 时不渲染该区块 |
| `striped` | bool | `false` | 是否斑马纹；源码用法：`'progress-bar-striped' => $bar['striped'] ?? $this->get('striped'),` |
| `animated` | bool | `false` | 源码用法：`'progress-bar-animated' => $bar['animated'] ?? $this->get('animated'),` |
| `label` | mixed | `null` | 标签文案（表单字段标签 / 按钮文案）；源码用法：`'label' => $this->get('label'),` |
| `bars` | array | `[]` | 柱状数据数组 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `spinner`

加载旋转指示器（border/grow 变体）。

> **类**：`zxf\XfAdmin\Components\UI\Spinner`
> **文件**：`src/Components/UI/Spinner.php`（55 行）
> **依赖插件**：`spinkit`

**用法示例**

```php
XfAdmin::spinner(['type' => 'border', 'variant' => 'primary', 'size' => 'sm']);
XfAdmin::spinner(['spinkit' => 'wave']);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::spinner([
    'type' => 'border',    // border | grow
    'variant' => 'primary',
    'size' => null,    // sm
    'spinkit' => null,
]);
```

</details>

> **渲染骨架**：主要 class `sk-fold-cube` `visually-hidden` `spinner-`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `type` | string | `'border'` | 枚举白名单 `'border', 'grow'` |
| `variant` | string | `'primary'` | 可选值：`primary` |
| `size` | mixed | `null` | 枚举白名单 `'sm'` |
| `spinkit` | mixed | `null` | **文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `pagination`

分页器（页码/上一页/下一页）。

> **类**：`zxf\XfAdmin\Components\UI\Pagination`
> **文件**：`src/Components/UI/Pagination.php`（89 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::pagination([
    'total'    => 200,        // 总条数
    'per_page' => 10,
    'current'  => 3,
    'url'      => '/users?page={page}',   // {page} 占位
    'size'     => null,       // sm | lg
    'align'    => 'center',   // start | center | end
    'rounded'  => false,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::pagination([
    'total' => 0,
    'per_page' => 15,
    'current' => 1,
    'url' => '?page={page}',
    'window' => 2,
    'size' => null,
    'align' => 'center',
    'rounded' => false,
    'arrows' => true,
]);
```

</details>

> **渲染骨架**：主要 class `page-link` `pagination` `page-item`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `total` | int | `0` | 合计数值；源码用法：`$total = (int) $this->get('total');` |
| `per_page` | int | `15` | 源码用法：`$perPage = max(1, (int) $this->get('per_page'));` |
| `current` | int | `1` | 源码用法：`$current = min(max(1, (int) $this->get('current')), $pages);` |
| `url` | string | `'?page={page}'` | 链接地址（自动做安全协议校验）；源码用法：`return str_replace('{page}', (string) $page, (string) $this->safeUrl($this->get('url'…` |
| `window` | int | `2` | 源码用法：`$window = (int) $this->get('window');` |
| `size` | mixed | `null` | 尺寸：sm \| lg（部分组件支持 md/xl）；源码用法：`], ($this->get('size') && ($pz = $this->enum($this->get('size'), self::ENUM_SIZE, '')…` |
| `align` | string | `'center'` | 对齐方式：start \| center \| end；源码用法：`'justify-content-center' => $this->get('align') === 'center',` |
| `rounded` | bool | `false` | 源码用法：`'pagination-rounded' => $this->get('rounded'),` |
| `arrows` | bool | `true` | 开关：非空 / 真值时启用对应区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `listGroup`

列表组（卡片式条目列表）。

> **类**：`zxf\XfAdmin\Components\UI\ListGroup`
> **文件**：`src/Components/UI/ListGroup.php`（62 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::listGroup([
    'items' => [
        ['text' => '项目一', 'active' => true],
        ['text' => '项目二', 'url' => '/x', 'badge' => ['text' => '3', 'class' => 'bg-danger']],
        ['content' => '<b>自定义HTML</b>'],
    ],
    'flush'     => false,
    'numbered'  => false,
    'horizontal'=> false,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::listGroup([
    'items' => [],
    'flush' => false,
    'numbered' => false,
    'horizontal' => false,
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`url`

> **渲染骨架**：主要 class `list-group` `list-group-item`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `flush` | bool | `false` | 是否无边框（list-group-flush）；源码用法：`'list-group-flush' => $this->get('flush'),` |
| `numbered` | bool | `false` | 源码用法：`'list-group-numbered' => $this->get('numbered'),` |
| `horizontal` | bool | `false` | 是否水平排列；源码用法：`'list-group-horizontal' => $this->get('horizontal'),` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `avatar`

头像（图片/文字/图标/状态点）。

> **类**：`zxf\XfAdmin\Components\UI\Avatar`
> **文件**：`src/Components/UI/Avatar.php`（87 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::avatar(['src' => '/a.jpg', 'size' => 'md', 'rounded' => 'circle']);
XfAdmin::avatar(['text' => 'ZS', 'variant' => 'primary', 'size' => 'lg']);
XfAdmin::avatar(['group' => [['src' => '/a.jpg'], ['text' => '+5', 'variant' => 'info']]]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::avatar([
    'src' => null,
    'text' => null,
    'icon' => null,
    'alt' => '',
    'size' => 'md',    // xs | sm | md | lg | xl | xxl
    'rounded' => 'circle',    // circle | 0 | 1 | 2 | 3
    'variant' => 'primary',
    'soft' => true,
    'group' => [],
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `group[]` 元素键：`size`（默认 `md`）、`rounded`（默认 `circle`）、`src`、`alt`、`variant`（默认 `primary`）、`soft`、`icon`、`text`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `src` | mixed | `null` | 资源地址（图片 / iframe / 文件） |
| `text` | mixed | `null` | 正文/按钮文案（纯文本语义，输出时转义） |
| `icon` | mixed | `null` | Tabler 图标 class，如 `ti ti-user` |
| `alt` | string | `''` | 图片替代文本 |
| `size` | string | `'md'` | xs \| sm \| md \| lg \| xl \| xxl |
| `rounded` | string | `'circle'` | circle \| 0 \| 1 \| 2 \| 3 |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束） |
| `soft` | bool | `true` | 柔和（浅色底）样式 |
| `group` | array | `[]` | 分组信息 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `avatarGroup`

头像组（重叠堆叠展示多人）。

> **类**：`zxf\XfAdmin\Components\UI\AvatarGroup`
> **文件**：`src/Components/UI/AvatarGroup.php`（47 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::avatarGroup([
    'users' => [
        ['src' => 'users/user-1.jpg', 'name' => '张三', 'status' => 'online'],
        ['name' => '李四', 'status' => 'busy'],     // 无 src 显示首字母
    ],
    'max'   => 5,          // 最多显示数量，超出显示 +N
    'size'  => 'sm',       // sm | '' | lg
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::avatarGroup([
    'users' => [],
    'max' => 5,
    'size' => '',
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `users` | array | `[]` | 用户列表 |
| `max` | int | `5` | 最大值；源码用法：`$max = max(1, (int) $this->get('max'));` |
| `size` | string | `''` | 尺寸：sm \| lg（部分组件支持 md/xl）；源码用法：`$size = (string) $this->get('size');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `icon`

图标（Tabler 字体图标封装）。

> **类**：`zxf\XfAdmin\Components\UI\Icon`
> **文件**：`src/Components/UI/Icon.php`（43 行）
> **依赖插件**：`lucide`

**用法示例**

```php
XfAdmin::icon(['name' => 'home']);
XfAdmin::icon(['name' => 'settings', 'lib' => 'lucide']);
XfAdmin::icon(['name' => 'bell', 'size' => 'fs-24', 'color' => 'text-primary']);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::icon([
    'name' => '',
    'lib' => 'tabler',    // tabler | lucide
    'size' => null,    // fs-* 类
    'color' => null,    // text-* 类
]);
```

</details>

> **渲染骨架**：主要 class `ti` `ti-`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `name` | string | `''` | 表单字段名 / 语义名称；输出到 `` 属性 |
| `lib` | string | `'tabler'` | tabler \| lucide；开关：非空 / 真值时启用对应区块 |
| `size` | mixed | `null` | fs-* 类 |
| `color` | mixed | `null` | text-* 类 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `iconSet`

图标集展示（Tabler 图标网格）。

> **类**：`zxf\XfAdmin\Components\UI\IconSet`
> **文件**：`src/Components/UI/IconSet.php`（92 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::iconSet([
    'set' => 'tabler',  // tabler | flag | lucide
    'icons' => ['ti-home', 'ti-user', 'ti-settings', 'ti-search', ...],
    'columns' => 6,
    'searchable' => true,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::iconSet([
    'set' => 'tabler',
    'icons' => [],
    'columns' => 6,
    'searchable' => true,
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `icons[]` 元素键：`name`

> **渲染骨架**：主要 class `mb-4` `input-group` `input-group-text` `col` `card` `xf-icon-card` `border` `card-body`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `set` | string | `'tabler'` | 集合 / 预设值 |
| `icons` | array | `[]` | 图标列表 |
| `columns` | int | `6` | 列定义数组 |
| `searchable` | bool | `true` | 是否参与搜索 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `toast`

轻提示（右上角自动消失吐司）。

> **类**：`zxf\XfAdmin\Components\UI\Toast`
> **文件**：`src/Components/UI/Toast.php`（59 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::toast(['title' => '通知', 'body' => '保存成功', 'variant' => 'success', 'autohide' => true, 'show' => true]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::toast([
    'title' => null,
    'body' => '',
    'time' => null,
    'variant' => null,
    'autohide' => true,
    'delay' => 5000,
    'show' => true,
    'placement' => null,    // 如 'top-0 end-0'，非空时包裹固定容器
]);
```

</details>

> **渲染骨架**：主要 class `toast-header` `toast-body` `d-flex` `toast`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | mixed | `null` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `time` | mixed | `null` | 时间文本；**文本槽位**：输出前自动 HTML 转义；为 `null` 时不渲染该区块 |
| `variant` | mixed | `null` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；**文本槽位**：输出前自动 HTML 转义；为 `null` 时不渲染该区块 |
| `autohide` | bool | `true` | 输出到 `` 属性 |
| `delay` | int | `5000` | 延迟（毫秒）；源码用法：`'data-bs-delay' => (string) $this->get('delay'),` |
| `show` | bool | `true` | 源码用法：`'class' => Html::cls('toast', ['show' => $this->get('show')], $variant ? 'text-bg-' .…` |
| `placement` | mixed | `null` | 如 'top-0 end-0'，非空时包裹固定容器；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `timeline`

时间线（纵向/横向事件流）。

> **类**：`zxf\XfAdmin\Components\UI\Timeline`
> **文件**：`src/Components/UI/Timeline.php`（52 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::timeline([
    'items' => [
        ['time' => '09:30', 'title' => '创建订单', 'text' => '订单 #1234 已创建', 'icon' => 'ti ti-plus', 'variant' => 'primary'],
        ['time' => '10:00', 'title' => '已付款', 'variant' => 'success'],
    ],
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::timeline([
    'items' => [],
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`variant`（默认 `primary`）、`icon`（默认 `ti ti-point-filled`）、`time`、`title`、`text`、`content`

> **渲染骨架**：主要 class `timeline-item` `d-flex` `align-items-stretch` `timeline-time` `pe-3` `text-muted` `timeline-dot` `flex-shrink-0`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `carousel`

轮播图（多图滑动，object-fit 封面）。

> **类**：`zxf\XfAdmin\Components\UI\Carousel`
> **文件**：`src/Components/UI/Carousel.php`（74 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::carousel([
    'items' => [
        ['image' => '/1.jpg', 'caption' => '<h5>标题</h5><p>描述</p>'],
        ['content' => '<div>自定义任意HTML</div>'],
    ],
    'indicators' => true,
    'controls'   => true,
    'fade'       => false,
    'interval'   => 5000,
    'dark'       => false,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::carousel([
    'items' => [],
    'indicators' => false,
    'controls' => true,
    'fade' => false,
    'interval' => 5000,
    'dark' => false,
    'ride' => 'carousel',
    'height' => '',    // 轮播图高度，留空则由 .xf-carousel img 兜底（响应式）
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]`：`image`（图片地址）、`alt`、`caption`（标题）、`content`（内容）、`interval`（停留毫秒）

> **渲染骨架**：主要 class `carousel-indicators` `carousel-inner` `carousel-caption` `d-none` `d-md-block` `carousel-control-prev-icon` `visually-hidden` `carousel-control-next-icon`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `indicators` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `controls` | bool | `true` | 控件配置（播放器 / 轮播控件等）；开关：非空 / 真值时启用对应区块 |
| `fade` | bool | `false` | 是否启用淡入动画；源码用法：`'class' => Html::cls('carousel slide', ['carousel-fade' => $this->get('fade'), 'carou…` |
| `interval` | int | `5000` | 间隔时间（毫秒）；源码用法：`$html .= '<div class="' . Html::cls('carousel-item', ['active' => $i === 0]) . '" dat…` |
| `dark` | bool | `false` | 源码用法：`'class' => Html::cls('carousel slide', ['carousel-fade' => $this->get('fade'), 'carou…` |
| `ride` | string | `'carousel'` | 输出到 `` 属性 |
| `height` | string | `''` | 轮播图高度，留空则由 .xf-carousel img 兜底（响应式）；CSS 长度：仅接受 `数字+单位`（px/%/rem/em/vh/vw/pt/ch/fr） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `breadcrumb`

面包屑导航。

> **类**：`zxf\XfAdmin\Components\UI\Breadcrumb`
> **文件**：`src/Components/UI/Breadcrumb.php`（42 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::breadcrumb(['items' => [['text' => '首页', 'url' => '/'], ['text' => '列表']]]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::breadcrumb([
    'items' => [],
    'divider' => null,    // 自定义分隔符字符
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`active`、`url`、`text`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `divider` | mixed | `null` | 自定义分隔符字符；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `tooltip`

文字提示气泡（hover 触发）。

> **类**：`zxf\XfAdmin\Components\UI\Tooltip`
> **文件**：`src/Components/UI/Tooltip.php`（51 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::tooltip([
    'text'      => '按钮文字',           // 触发元素内容（HTML）
    'title'     => '这是提示内容',
    'placement' => 'top',               // top|bottom|left|right
    'tag'       => 'button',            // 触发元素标签
    'class'     => 'btn btn-primary',
    'html'      => false,               // title 是否允许 HTML
    'trigger'   => null,               // hover focus click
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::tooltip([
    'text' => '',
    'title' => '',
    'placement' => 'top',
    'tag' => 'button',
    'class' => 'btn btn-secondary',
    'html' => false,
    'trigger' => null,
    'custom_class' => null,
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义 |
| `placement` | string | `'top'` | 弹出方位：top \| bottom \| left \| right \| start \| end；**文本槽位**：输出前自动 HTML 转义 |
| `tag` | string | `'button'` | 标签 / 渲染标签名；源码用法：`$tag = preg_replace('/[^a-z0-9]/i', '', (string) $this->get('tag')) ?: 'button';` |
| `class` | string | `'btn btn-secondary'` | 附加到根元素的自定义 class；源码用法：`'class' => Html::cls($this->get('class')),` |
| `html` | bool | `false` | 自定义 HTML（原样输出）；开关：非空 / 真值时启用对应区块 |
| `trigger` | mixed | `null` | 触发方式（hover / click / focus），或触发按钮文案；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `custom_class` | mixed | `null` | **文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `popover`

弹出框（hover/click 富内容）。

> **类**：`zxf\XfAdmin\Components\UI\Popover`
> **文件**：`src/Components/UI/Popover.php`（52 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::popover([
    'text'      => '点击我',
    'title'     => '弹框标题',
    'content'   => '弹框正文内容',
    'placement' => 'right',
    'trigger'   => 'click',   // click|hover|focus
    'tag'       => 'button',
    'class'     => 'btn btn-primary',
    'html'      => false,
    'dismiss'   => false,     // true 时点击外部关闭
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::popover([
    'text' => '',
    'title' => null,
    'content' => '',
    'placement' => 'top',
    'trigger' => 'click',
    'tag' => 'button',
    'class' => 'btn btn-secondary',
    'html' => false,
    'dismiss' => false,
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `title` | mixed | `null` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `content` | string | `''` | 内容区（可为 HTML 字符串、组件实例或数组）；**文本槽位**：输出前自动 HTML 转义 |
| `placement` | string | `'top'` | 弹出方位：top \| bottom \| left \| right \| start \| end；**文本槽位**：输出前自动 HTML 转义 |
| `trigger` | string | `'click'` | 触发方式（hover / click / focus），或触发按钮文案；**文本槽位**：输出前自动 HTML 转义 |
| `tag` | string | `'button'` | 标签 / 渲染标签名；源码用法：`$tag = preg_replace('/[^a-z0-9]/i', '', (string) $this->get('tag')) ?: 'button';` |
| `class` | string | `'btn btn-secondary'` | 附加到根元素的自定义 class；源码用法：`'class' => Html::cls($this->get('class')),` |
| `html` | bool | `false` | 自定义 HTML（原样输出）；开关：非空 / 真值时启用对应区块 |
| `dismiss` | bool | `false` | 是否可关闭（警告条 / 模态框）；输出到 `` 属性 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `placeholder`

占位符块（Bootstrap placeholder 闪烁）。

> **类**：`zxf\XfAdmin\Components\UI\Placeholder`
> **文件**：`src/Components/UI/Placeholder.php`（45 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::placeholder([
    'lines'   => [12, 6, 8, 12],   // 每行占用的栅格列数
    'glow'    => true,             // glow|wave 动画
    'animation' => 'glow',
    'variant' => null,            // primary/secondary...
    'size'    => null,            // xs|sm|lg
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::placeholder([
    // lines
    'lines' => [
        12,
        8,
        10,
        6,
    ],
    'animation' => 'glow',    // glow|wave|null
    'variant' => null,
    'size' => null,    // xs|sm|lg
    'body' => null,
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `lines` | array | `[12, 8, 10, 6]` | 数组结构（见「全参数示例」） |
| `animation` | string | `'glow'` | glow\|wave\|null |
| `variant` | mixed | `null` | 可选值：`primary` |
| `size` | mixed | `null` | 枚举白名单 `'xs', 'sm', 'lg'` |
| `body` | mixed | `null` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `collapse`

折叠面板（单/多展开）。

> **类**：`zxf\XfAdmin\Components\UI\Collapse`
> **文件**：`src/Components/UI/Collapse.php`（61 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::collapse([
    'trigger'   => '切换显示',      // 触发按钮内容
    'body'      => '被折叠的内容',
    'open'      => false,
    'trigger_class' => 'btn btn-primary',
    'horizontal'=> false,
    'card'      => true,           // 内容是否包裹卡片
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::collapse([
    'trigger' => '展开/收起',
    'body' => '',
    'open' => false,
    'trigger_tag' => 'button',
    'trigger_class' => 'btn btn-primary',
    'horizontal' => false,
    'card' => true,
]);
```

</details>

> **渲染骨架**：主要 class `card` `card-body` `mt-2` `mb-0` `collapse`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `trigger` | string | `'展开/收起'` | 触发方式（hover / click / focus），或触发按钮文案；**文本槽位**：输出前自动 HTML 转义 |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `open` | bool | `false` | 源码用法：`$open = (bool) $this->get('open');` |
| `trigger_tag` | string | `'button'` | 源码用法：`$triggerTag = $this->get('trigger_tag') === 'a' ? 'a' : 'button';` |
| `trigger_class` | string | `'btn btn-primary'` | 源码用法：`'class' => Html::cls($this->get('trigger_class')),` |
| `horizontal` | bool | `false` | 是否水平排列；源码用法：`'collapse-horizontal' => (bool) $this->get('horizontal'),` |
| `card` | bool | `true` | 是否以卡片容器呈现（部分组件为遗留键）；源码用法：`$inner = $this->get('card')` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `scrollspy`

滚动监听（导航高亮当前区块）。

> **类**：`zxf\XfAdmin\Components\UI\Scrollspy`
> **文件**：`src/Components/UI/Scrollspy.php`（62 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::scrollspy([
    'items'  => [
        ['id' => 'sec1', 'label' => '第一节', 'content' => '...'],
        ['id' => 'sec2', 'label' => '第二节', 'content' => '...'],
    ],
    'height' => '250px',
    'nav_width' => 3,   // 左侧导航栅格
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::scrollspy([
    'items' => [],
    'height' => '250px',
    'nav_width' => 3,
    'offset' => 0,
    'smooth' => true,
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`id`、`label`

> **渲染骨架**：主要 class `nav-link` `mb-3`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `height` | string | `'250px'` | 高度（CSS 长度，受安全白名单约束）；CSS 长度：仅接受 `数字+单位`（px/%/rem/em/vh/vw/pt/ch/fr） |
| `nav_width` | int | `3` | 源码用法：`$navW = max(1, min(6, (int) $this->get('nav_width')));` |
| `offset` | int | `0` | 栅格偏移；源码用法：`'data-bs-offset' => (int) $this->get('offset'),` |
| `smooth` | bool | `true` | 输出到 `` 属性 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `ratio`

固定宽高比容器（视频/嵌入比例）。

> **类**：`zxf\XfAdmin\Components\UI\Ratio`
> **文件**：`src/Components/UI/Ratio.php`（64 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::ratio([
    'ratio'  => '16x9',              // 1x1|4x3|16x9|21x9 或自定义百分比数字
    'src'    => 'https://...',       // iframe/video 源
    'type'   => 'iframe',            // iframe|video|content
    'body'   => '自定义内容',
    'allowfullscreen' => true,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::ratio([
    'ratio' => '16x9',
    'src' => null,
    'type' => 'iframe',
    'body' => null,
    'allowfullscreen' => true,
    'controls' => true,
]);
```

</details>

> **渲染骨架**：主要 class `d-flex` `align-items-center` `justify-content-center` `w-100` `h-100` `bg-body-tertiary` `text-muted` `ratio`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `ratio` | string | `'16x9'` | 宽高比（如 `16/9`）；源码用法：`$ratio = (string) $this->get('ratio');` |
| `src` | mixed | `null` | 资源地址（图片 / iframe / 文件）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `type` | string | `'iframe'` | 类型（各组件语义不同，详见该组件说明）；源码用法：`if (! $this->get('src') && $this->get('type') !== 'content') {` |
| `body` | mixed | `null` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；开关：非空 / 真值时启用对应区块 |
| `allowfullscreen` | bool | `true` | 源码用法：`$fs = $this->get('allowfullscreen') ? ' allowfullscreen' : '';` |
| `controls` | bool | `true` | 控件配置（播放器 / 轮播控件等）；源码用法：`$ctrl = $this->get('controls') ? ' controls' : '';` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `rating`

评分（星星/点赞，只读/可交互）。

> **类**：`zxf\XfAdmin\Components\UI\Rating`
> **文件**：`src/Components/UI/Rating.php`（54 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::rating([
    'value'   => 3.5,
    'max'     => 5,
    'variant' => 'warning',
    'size'    => null,          // fs-3 等
    'show_value' => true,
    'count'   => 128,           // 评价数
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::rating([
    'value' => 0,
    'max' => 5,
    'variant' => 'warning',
    'size' => null,
    'show_value' => false,
    'count' => null,
    'icon_full' => 'ti ti-star-filled',
    'icon_half' => 'ti ti-star-half-filled',
    'icon_empty' => 'ti ti-star',
]);
```

</details>

> **渲染骨架**：主要 class `ms-1` `text-body` `fw-medium` `text-muted` `d-inline-flex` `align-items-center` `gap-1` `text-`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `value` | int | `0` | 当前值（表单控件值 / 展示数值）；源码用法：`$value = (float) $this->get('value');` |
| `max` | int | `5` | 最大值；源码用法：`$max = (int) $this->get('max');` |
| `variant` | string | `'warning'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；**文本槽位**：输出前自动 HTML 转义 |
| `size` | mixed | `null` | 尺寸：sm \| lg（部分组件支持 md/xl）；**文本槽位**：输出前自动 HTML 转义 |
| `show_value` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `count` | mixed | `null` | 数量 / 计数徽标数字；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `icon_full` | string | `'ti ti-star-filled'` | 源码用法：`$icon = $this->get('icon_full');` |
| `icon_half` | string | `'ti ti-star-half-filled'` | 源码用法：`$icon = $this->get('icon_half');` |
| `icon_empty` | string | `'ti ti-star'` | 源码用法：`$icon = $this->get('icon_empty');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `ribbon`

缎带角标（卡片右上角标签）。

> **类**：`zxf\XfAdmin\Components\UI\Ribbon`
> **文件**：`src/Components/UI/Ribbon.php`（37 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::ribbon([
    'text'    => '推荐',
    'variant' => 'danger',
    'position'=> 'left',        // left|right
    'body'    => '卡片内容',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::ribbon([
    'text' => '',
    'variant' => 'primary',
    'position' => 'left',
    'body' => '',
    'shape' => null,    // null|round
]);
```

</details>

> **渲染骨架**：主要 class `ribbon-content`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义 |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；**文本槽位**：输出前自动 HTML 转义 |
| `position` | string | `'left'` | 位置；源码用法：`$pos = $this->get('position') === 'right' ? 'end' : 'start';` |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组） |
| `shape` | mixed | `null` | null\|round |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `chip`

筹码标签（可删除的紧凑标签）。

> **类**：`zxf\XfAdmin\Components\UI\Chip`
> **文件**：`src/Components/UI/Chip.php`（51 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::chip([
    'label'    => '张三',
    'avatar'   => 'users/avatar-1.jpg',
    'icon'     => null,
    'variant'  => 'light',
    'dismissible' => true,
    'href'     => null,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::chip([
    'label' => '',
    'avatar' => null,
    'icon' => null,
    'variant' => 'light',
    'dismissible' => false,
    'href' => null,
]);
```

</details>

> **渲染骨架**：主要 class `avatar` `avatar-xs` `flex-shrink-0` `badge` `bg-`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `label` | string | `''` | 标签文案（表单字段标签 / 按钮文案）；**文本槽位**：输出前自动 HTML 转义 |
| `avatar` | mixed | `null` | 头像地址（自动解析为包内图片 URL）；开关：非空 / 真值时启用对应区块 |
| `icon` | mixed | `null` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块 |
| `variant` | string | `'light'` | 可选值：`primary` |
| `dismissible` | bool | `false` | 开关：非空 / 真值时启用对应区块 |
| `href` | mixed | `null` | 链接地址（同 url）；URL：经协议白名单校验（拦截 `javascript:` 等）；开关：非空 / 真值时启用对应区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `stepper`

步骤条（水平步骤指示器，向导导航）。

> **类**：`zxf\XfAdmin\Components\UI\Stepper`
> **文件**：`src/Components/UI/Stepper.php`（50 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::stepper([
    'steps' => [
        ['title' => '已下单', 'text' => '10:00', 'status' => 'done'],
        ['title' => '已发货', 'text' => '12:00', 'status' => 'active'],
        ['title' => '已签收', 'status' => 'pending'],
    ],
    'vertical' => false,
    'variant'  => 'primary',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::stepper([
    'steps' => [],
    'vertical' => false,
    'variant' => 'primary',
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `steps[]` 元素键：`status`（默认 `pending`）、`title`、`text`

> **渲染骨架**：主要 class `xf-stepper-body` `xf-stepper`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `steps` | array | `[]` | 步骤数组（向导 / 步骤条） |
| `vertical` | bool | `false` | 是否纵向排列；源码用法：`$vertical = (bool) $this->get('vertical');` |
| `variant` | string | `'primary'` | 可选值：`primary` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `descriptionList`

描述列表（术语/定义键值对）。

> **类**：`zxf\XfAdmin\Components\UI\DescriptionList`
> **文件**：`src/Components/UI/DescriptionList.php`（50 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::descriptionList([
    'items' => [
        '订单号' => '#12345',
        '状态'   => XfAdmin::badge(['text' => '已支付', 'variant' => 'success']),
        ['label' => '备注', 'value' => '尽快发货', 'raw' => true],
    ],
    'horizontal' => true,
    'label_width'=> 4,          // 水平布局左侧栅格
    'striped'    => false,
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::descriptionList([
    'items' => [],
    'horizontal' => true,
    'label_width' => 4,
    'striped' => false,
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`label`、`value`

> **渲染骨架**：主要 class `mb-0`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `items` | array | `[]` | 条目数组（结构见各组件说明） |
| `horizontal` | bool | `true` | 是否水平排列；源码用法：`$horizontal = (bool) $this->get('horizontal');` |
| `label_width` | int | `4` | 源码用法：`$lw = max(1, min(6, (int) $this->get('label_width')));` |
| `striped` | bool | `false` | 是否斑马纹 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `loadingButton`

加载按钮（点击后显示 spinner）。

> **类**：`zxf\XfAdmin\Components\UI\LoadingButton`
> **文件**：`src/Components/UI/LoadingButton.php`（64 行）
> **依赖插件**：`ladda`

**用法示例**

```php
XfAdmin::loadingButton([
    'text'    => '保存',
    'variant' => 'primary',
    'driver'  => 'spinner',   // spinner | ladda
    'type'    => 'submit',
    'size'    => '',          // lg | sm
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::loadingButton([
    'text' => '提交',
    'variant' => 'primary',
    'driver' => 'spinner',
    'type' => 'button',
    'size' => '',
    'name' => null,
    'value' => null,
    'icon' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-lbtn-label` `xf-lbtn-spinner` `spinner-border` `spinner-border-sm` `d-none` `btn` `btn-`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `'提交'` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义 |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`'class' => Html::cls('btn btn-' . $this->enum($this->get('variant'), array_merge(self…` |
| `driver` | string | `'spinner'` | 底层驱动（如编辑器 quill/summernote、上传 native/dropzone/filepond）；源码用法：`return $this->get('driver') === 'ladda' ? ['ladda'] : [];` |
| `type` | string | `'button'` | 类型（各组件语义不同，详见该组件说明）；输出到 `` 属性 |
| `size` | string | `''` | 可选值：`lg` |
| `name` | mixed | `null` | 表单字段名 / 语义名称；开关：非空 / 真值时启用对应区块 |
| `value` | mixed | `null` | 当前值（表单控件值 / 展示数值）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `icon` | string | `''` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `divider`

分割线（文本/图标居中分割）。

> **类**：`zxf\XfAdmin\Components\UI\Divider`
> **文件**：`src/Components/UI/Divider.php`（37 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::divider([
    'text'    => '或',            // 中间文字（留空则为纯线条）
    'icon'    => 'ti ti-point',   // 可选中间图标
    'variant' => '',              // 可选颜色：primary/success/danger/...（用于线条着色）
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::divider([
    'text' => '',
    'icon' => '',
    'variant' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-divider-inner`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；源码用法：`$text = (string) $this->get('text');` |
| `icon` | string | `''` | Tabler 图标 class，如 `ti ti-user`；源码用法：`$icon = (string) $this->get('icon');` |
| `variant` | string | `''` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`$variant = (string) $this->get('variant');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `kbd`

键盘按键标记（<kbd> 样式）。

> **类**：`zxf\XfAdmin\Components\UI\Kbd`
> **文件**：`src/Components/UI/Kbd.php`（35 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::kbd([
    'keys' => ['Ctrl', 'K'],   // 多个按键以 + 连接
    'text' => '',              // 或直接使用 text（原样输出）
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::kbd([
    'keys' => [],
    'text' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-kbd-plus`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `keys` | array | `[]` | 键名数组 |
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；源码用法：`$text = (string) $this->get('text');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `media`

媒体对象（左图右文列表项）。

> **类**：`zxf\XfAdmin\Components\UI\Media`
> **文件**：`src/Components/UI/Media.php`（59 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::media([
    'image'  => 'users/user-1.jpg',
    'avatar' => false,         // true 时图片作为圆形头像
    'title'  => '标题',
    'text'   => '正文内容',
    'meta'   => '副信息',
    'href'   => '#',           // 提供时整体可点击
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::media([
    'image' => '',
    'avatar' => false,
    'title' => '',
    'text' => '',
    'meta' => '',
    'href' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-media` `d-flex` `gap-3` `avatar` `avatar-sm` `flex-shrink-0` `xf-media-body` `flex-grow-1`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `image` | string | `''` | 图片地址（支持外链 / data URI / 包内 images 相对路径）；源码用法：`$img = (string) $this->get('image');` |
| `avatar` | bool | `false` | 头像地址（自动解析为包内图片 URL）；开关：非空 / 真值时启用对应区块 |
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题）；源码用法：`$title = (string) $this->get('title');` |
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；源码用法：`$text = (string) $this->get('text');` |
| `meta` | string | `''` | 附加信息（时间 / 作者等）；源码用法：`$meta = (string) $this->get('meta');` |
| `href` | string | `''` | 链接地址（同 url）；URL：经协议白名单校验（拦截 `javascript:` 等） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `skeleton`

骨架屏占位（加载占位动画）。

> **类**：`zxf\XfAdmin\Components\UI\Skeleton`
> **文件**：`src/Components/UI/Skeleton.php`（52 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::skeleton([
    'lines'    => 3,        // 文本占位行数（type=text 时生效）
    'type'     => 'text',   // text | circle | rect
    'width'    => '100%',   // 占位宽度
    'height'   => 16,       // rect 时高度
    'circle'   => 48,       // circle 直径（覆盖 height）
    'animated' => true,     // 是否启用微光动画
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::skeleton([
    'lines' => 3,
    'type' => 'text',
    'width' => '100%',
    'height' => 16,
    'circle' => 48,
    'animated' => true,
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `lines` | int | `3` | 源码用法：`$n = max(1, (int) $this->get('lines'));` |
| `type` | string | `'text'` | 类型（各组件语义不同，详见该组件说明）；源码用法：`$type = (string) $this->get('type');` |
| `width` | string | `'100%'` | 宽度（数字=栅格列数或 CSS 长度）；**文本槽位**：输出前自动 HTML 转义 |
| `height` | int | `16` | 高度（CSS 长度，受安全白名单约束）；源码用法：`$h = (int) $this->get('height');` |
| `circle` | int | `48` | 源码用法：`$d = (int) $this->get('circle');` |
| `animated` | bool | `true` | 源码用法：`$animated = $this->get('animated') ? ' xf-skeleton-animated' : '';` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `switch`

开关切换（Toggle，PHP 保留字故类名 Toggle）。

> **类**：`zxf\XfAdmin\Components\UI\Toggle`
> **文件**：`src/Components/UI/Toggle.php`（54 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::switch([
    'name'     => 'notify',          // 表单字段名
    'checked'  => true,              // 是否默认开启
    'disabled' => false,
    'label'    => '开启通知',         // 右侧文字标签（可空）
    'value'    => '1',
    'variant'  => 'primary',         // primary/success/danger/warning/info（通过 accent-color 着色）
    'size'     => '',                // '' | 'sm' | 'lg'
  ]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::switch([
    'name' => '',
    'checked' => false,
    'disabled' => false,
    'label' => '',
    'value' => '1',
    'variant' => 'primary',
    'size' => '',
    'id' => null,
    'class' => '',
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `name` | string | `''` | 表单字段名 / 语义名称；**文本槽位**：输出前自动 HTML 转义；为 `null` 时不渲染该区块 |
| `checked` | bool | `false` | 源码用法：`. ($this->get('checked') ? ' checked' : '')` |
| `disabled` | bool | `false` | 是否禁用；源码用法：`. ($this->get('disabled') ? ' disabled' : '')` |
| `label` | string | `''` | 标签文案（表单字段标签 / 按钮文案）；**文本槽位**：输出前自动 HTML 转义；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `value` | string | `'1'` | 当前值（表单控件值 / 展示数值）；源码用法：`. ' value="' . $this->e((string) $this->get('value')) . '"'` |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；**文本槽位**：输出前自动 HTML 转义 |
| `size` | string | `''` | 尺寸：sm \| lg（部分组件支持 md/xl）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `id` | mixed | `null` | 根元素 id（留空自动生成唯一 id） |
| `class` | string | `''` | 附加到根元素的自定义 class；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `codeBlock`

代码块（语法高亮 + 复制按钮）。

> **类**：`zxf\XfAdmin\Components\UI\CodeBlock`
> **文件**：`src/Components/UI/CodeBlock.php`（56 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::codeBlock([
    'code'     => '<?php echo "hi";',  // 任意代码，自动 HTML 转义
    'language' => 'php',               // 语言标签（仅展示用）
    'title'    => '示例',              // 标题栏（可空）
    'copyable' => true,                // 显示复制按钮
    'theme'    => 'dark',              // dark | light
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::codeBlock([
    'code' => '',
    'language' => '',
    'title' => '',
    'copyable' => true,
    'theme' => 'dark',
    'id' => null,
    'class' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-code-header` `d-flex` `align-items-center` `justify-content-between` `xf-code-lang` `justify-content-end`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `code` | string | `''` | 源码用法：`. '>' . $this->e((string) $this->get('code')) . '</code></pre>'` |
| `language` | string | `''` | 语言包覆盖（DataTables）；**文本槽位**：输出前自动 HTML 转义；为 `null` 时不渲染该区块 |
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `copyable` | bool | `true` | 源码用法：`$copyBtn = $this->get('copyable')` |
| `theme` | string | `'dark'` | 主题（light / dark / 图表主题名）；**文本槽位**：输出前自动 HTML 转义 |
| `id` | mixed | `null` | 根元素 id（留空自动生成唯一 id） |
| `class` | string | `''` | 附加到根元素的自定义 class；为 `null` 时不渲染该区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `empty`

空状态组件（别名，对应 EmptyState）。

> **类**：`zxf\XfAdmin\Components\UI\EmptyState`
> **文件**：`src/Components/UI/EmptyState.php`（47 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::empty([
    'icon'  => 'ti ti-package',                 // Tabler 图标
    'title' => '暂无数据',
    'text'  => '还没有任何记录',
    'action'=> XfAdmin::button([...]),          // 操作区（原样输出 HTML）
    'size'  => '',                              // '' | 'sm' | 'lg'
  ]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::empty([
    'icon' => 'ti ti-package',
    'title' => '暂无数据',
    'text' => '',
    'action' => '',
    'size' => '',
    'id' => null,
    'class' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-empty-action` `mt-3`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `icon` | string | `'ti ti-package'` | Tabler 图标 class，如 `ti ti-user`；**文本槽位**：输出前自动 HTML 转义 |
| `title` | string | `'暂无数据'` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义 |
| `text` | string | `''` | 正文/按钮文案（纯文本语义，输出时转义）；**文本槽位**：输出前自动 HTML 转义；为 `null` 时不渲染该区块 |
| `action` | string | `''` | 表单提交地址 / 动作类型；**内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；为 `null` 时不渲染该区块 |
| `size` | string | `''` | 尺寸：sm \| lg（部分组件支持 md/xl）；源码用法：`$sz = $this->enum($this->get('size'), self::ENUM_SIZE, '');` |
| `id` | mixed | `null` | 根元素 id（留空自动生成唯一 id） |
| `class` | string | `''` | 附加到根元素的自定义 class；为 `null` 时不渲染该区块 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `toolbar`

工具栏容器（左右分栏操作区）。

> **类**：`zxf\XfAdmin\Components\UI\Toolbar`
> **文件**：`src/Components/UI/Toolbar.php`（39 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::toolbar([
    'left'  => XfAdmin::button([...]) . XfAdmin::searchBox([...]),  // 左区（原样 HTML）
    'right' => XfAdmin::button([...]),                              // 右区（原样 HTML）
    'class' => '',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::toolbar([
    'left' => '',
    'right' => '',
    'class' => '',
    'id' => null,
]);
```

</details>

> **渲染骨架**：主要 class `xf-toolbar-left` `d-flex` `align-items-center` `gap-2` `flex-wrap` `xf-toolbar-right`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `left` | string | `''` | **内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；为 `null` 时不渲染该区块 |
| `right` | string | `''` | **内容槽位**：`raw()` 原样输出（可传 HTML / 组件 / 闭包 / 数组）；为 `null` 时不渲染该区块 |
| `class` | string | `''` | 附加到根元素的自定义 class；为 `null` 时不渲染该区块 |
| `id` | mixed | `null` | 根元素 id（留空自动生成唯一 id） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `searchBox`

搜索框（输入+按钮/图标）。

> **类**：`zxf\XfAdmin\Components\UI\SearchBox`
> **文件**：`src/Components/UI/SearchBox.php`（44 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::searchBox([
    'name'        => 'q',
    'value'       => '',
    'placeholder' => '搜索...',
    'size'        => '',          // '' | 'sm' | 'lg'
    'class'       => '',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::searchBox([
    'name' => 'q',
    'value' => '',
    'placeholder' => '搜索...',
    'size' => '',
    'class' => '',
    'id' => null,
]);
```

</details>

> **渲染骨架**：主要 class `input-group-text`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `name` | string | `'q'` | 表单字段名 / 语义名称；**文本槽位**：输出前自动 HTML 转义 |
| `value` | string | `''` | 当前值（表单控件值 / 展示数值）；源码用法：`. ' value="' . $this->e((string) $this->get('value')) . '"'` |
| `placeholder` | string | `'搜索...'` | 占位提示文案；**文本槽位**：输出前自动 HTML 转义 |
| `size` | string | `''` | 尺寸：sm \| lg（部分组件支持 md/xl）；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `class` | string | `''` | 附加到根元素的自定义 class；开关：非空 / 真值时启用对应区块；为 `null` 时不渲染该区块 |
| `id` | mixed | `null` | 根元素 id（留空自动生成唯一 id） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `colorPalette`

配色方案展示（色块+HEX 值）。

> **类**：`zxf\XfAdmin\Components\UI\ColorPalette`
> **文件**：`src/Components/UI/ColorPalette.php`（68 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::colorPalette([
    'groups' => [
        ['title' => 'Primary Colors', 'colors' => ['primary' => '#4e5bf2', 'secondary' => '#6c757d', 'success' => '#1ac168', 'info' => '#2ab5d2', 'warning' => '#f6c343', 'danger' => '#ef4866', 'dark' => '#212529', 'light' => '#f8f9fa']],
        ['title' => 'Extended Colors', 'colors' => ['indigo' => '#6610f2', 'purple' => '#6f42c1', 'pink' => '#d63384', 'red' => '#dc3545', 'orange' => '#fd7e14', 'yellow' => '#ffc107', 'green' => '#198754', 'teal' => '#20c997', 'cyan' => '#0dcaf0']],
    ],
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::colorPalette([
    'groups' => [],
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `groups[]` 元素键：`title`、`colors`

> **渲染骨架**：主要 class `row` `g-3` `col-md-3` `col-sm-6` `card` `card-body` `p-0` `p-2`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `groups` | array | `[]` | 分组数据（下拉分组 / 权限分组 / 设置分组） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `typography`

排版展示（display/标题/文本工具类示例）。

> **类**：`zxf\XfAdmin\Components\UI\Typography`
> **文件**：`src/Components/UI/Typography.php`（153 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::typography();
XfAdmin::typography(['blocks' => ['display', 'headings']]);
XfAdmin::typography([
    'blocks' => [
        ['title' => '自定义区块', 'body' => '<p>任意 HTML</p>'],        // 自定义区块
    ],
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::typography([
    'blocks' => [],    // 区块列表：字符串（built-in 键）或 ['title'=>..,'body'=>..]
    'grid' => true,    // 是否用 row/col 网格包裹
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `blocks[]` 元素键：`title`、`body`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `blocks` | array | `[]` | 区块列表：字符串（built-in 键）或 ['title'=>..,'body'=>..] |
| `grid` | bool | `true` | 是否用 row/col 网格包裹 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `utilities`

Bootstrap 工具类展示（间距/弹性/边框等）。

> **类**：`zxf\XfAdmin\Components\UI\Utilities`
> **文件**：`src/Components/UI/Utilities.php`（136 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::utilities();
XfAdmin::utilities(['blocks' => ['spacing', 'flex']]);
XfAdmin::utilities([
    'blocks' => [['title' => '自定义', 'body' => '<div class="p-3 bg-light">demo</div>']],
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::utilities([
    'blocks' => [],
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `blocks[]` 元素键：`title`、`body`

> **渲染骨架**：主要 class `col-lg-6` `container-xxl` `row`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `blocks` | array | `[]` | 内容区块数组 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `videoEmbed`

视频嵌入（响应式比例容器）。

> **类**：`zxf\XfAdmin\Components\UI\VideoEmbed`
> **文件**：`src/Components/UI/VideoEmbed.php`（81 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::videoEmbed([
    'src' => 'https://www.youtube.com/embed/abc123',
    'provider' => 'youtube', // youtube | vimeo | html5 | bilibili
    'title' => '视频标题',
    'description' => '视频描述文字',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::videoEmbed([
    'src' => '',
    'provider' => 'youtube',
    'title' => '',
    'description' => '',
    'ratio' => '16x9',
    'autoplay' => false,
    'allow' => 'accelerometer; autoplay; clipboard-write; encrypted-media…',
]);
```

</details>

> **渲染骨架**：主要 class `mb-3` `bg-dark` `d-flex` `align-items-center` `justify-content-center` `text-white` `text-center`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `src` | string | `''` | 资源地址（图片 / iframe / 文件） |
| `provider` | string | `'youtube'` | 服务提供者 / 地图瓦片源 |
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题） |
| `description` | string | `''` | 描述文本 |
| `ratio` | string | `'16x9'` | 宽高比（如 `16/9`） |
| `autoplay` | bool | `false` | 是否自动播放 |
| `allow` | string | `'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture'` | 允许的行为（如允许的文件类型 / 操作） |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `commandPalette`

命令面板（⌘K 快捷命令）。

> **类**：`zxf\XfAdmin\Components\UI\CommandPalette`
> **文件**：`src/Components/UI/CommandPalette.php`（61 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::commandPalette([
    'id'      => 'xf-cmd-palette',
    'title'   => '快捷命令',
    'placeholder' => '输入命令或搜索…',
    'hotkey'  => 'meta+k',          // 唤起快捷键：meta+k / ctrl+k
    'commands'=> [                  // 命令列表
        ['label' => '新建用户', 'icon' => 'ti ti-user-plus', 'hint' => 'U', 'url' => '/admin/users/create'],
        ['label' => '系统设置', 'icon' => 'ti ti-settings', 'action' => 'openSettings'], // action 由 XFAdmin.onCommand 订阅
    ],
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::commandPalette([
    'id' => 'xf-cmd-palette',
    'title' => '快捷命令',
    'placeholder' => '输入命令或搜索…',
    'hotkey' => 'meta+k',
    'commands' => [],
    'empty' => '未找到匹配的命令',
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `commands[]` 元素键：`label`、`icon`（默认 `ti ti-command`）、`hint`、`url`、`action`

> **渲染骨架**：主要 class `flex-grow-1` `text-start` `modal` `fade` `xf-cmd-palette` `modal-dialog` `modal-dialog-centered` `modal-sm`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `id` | string | `'xf-cmd-palette'` | 根元素 id（留空自动生成唯一 id）；**文本槽位**：输出前自动 HTML 转义 |
| `title` | string | `'快捷命令'` | 标题文本（部分组件为弹窗/tooltip 标题） |
| `placeholder` | string | `'输入命令或搜索…'` | 占位提示文案；**文本槽位**：输出前自动 HTML 转义 |
| `hotkey` | string | `'meta+k'` | 源码用法：`. json_encode(['id' => $id, 'hotkey' => $this->get('hotkey')], JSON_HEX_TAG \| JSON_HE…` |
| `commands` | array | `[]` | 源码用法：`$cmds = (array) $this->get('commands');` |
| `empty` | string | `'未找到匹配的命令'` | 空数据提示文案；**文本槽位**：输出前自动 HTML 转义 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `notificationCenter`

通知中心（消息抽屉）。

> **类**：`zxf\XfAdmin\Components\UI\NotificationCenter`
> **文件**：`src/Components/UI/NotificationCenter.php`（77 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::notificationCenter([
    'id'       => 'xf-notify-center',
    'title'    => '通知中心',
    'badge'    => 3,                 // 未读角标数
    'empty'    => '暂无新通知',
    'items'    => [                  // 通知项
        ['avatar' => '', 'icon' => 'ti ti-user-plus', 'variant' => 'primary',
         'title' => '新用户注册', 'text' => '张三 申请加入', 'time' => '2 分钟前', 'unread' => true,
         'url' => '/admin/users'],
    ],
    'footer'   => ['all' => '/admin/notifications', 'clear' => true],
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::notificationCenter([
    'id' => 'xf-notify-center',
    'title' => '通知中心',
    'badge' => 0,
    'empty' => '暂无新通知',
    'items' => [],
    // footer
    'footer' => [
        'all' => null,
        'clear' => true,
    ],
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `items[]` 元素键：`avatar`、`icon`（默认 `ti ti-bell`）、`variant`（默认 `primary`）、`title`、`text`、`time`、`unread`、`url`
- `footer[]` 元素键：`all`、`clear`

> **渲染骨架**：主要 class `d-flex` `gap-2` `flex-shrink-0` `flex-grow-1` `justify-content-between` `fw-semibold` `small` `text-muted`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `id` | string | `'xf-notify-center'` | 根元素 id（留空自动生成唯一 id）；**文本槽位**：输出前自动 HTML 转义 |
| `title` | string | `'通知中心'` | 标题文本（部分组件为弹窗/tooltip 标题）；**文本槽位**：输出前自动 HTML 转义 |
| `badge` | int | `0` | 徽标文本或 `['text'=>..,'class'=>..]`；源码用法：`$badge = (int) $this->get('badge');` |
| `empty` | string | `'暂无新通知'` | 空数据提示文案；**文本槽位**：输出前自动 HTML 转义 |
| `items` | array | `[]` | 条目数组（结构见各组件说明）；源码用法：`$items = (array) $this->get('items');` |
| `footer` | array | `['all'=>{…}, 'clear'=>true]` | 底部内容（原样输出）；源码用法：`$footer = (array) $this->get('footer');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `dropzoneUpload`

拖拽上传区（Dropzone 自定义版）。

> **类**：`zxf\XfAdmin\Components\UI\DropzoneUpload`
> **文件**：`src/Components/UI/DropzoneUpload.php`（64 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::dropzoneUpload([
    'id'        => 'xf-dropzone',
    'url'       => '/admin/api/upload',   // 上传端点（XFAdmin.request POST）
    'multiple'  => true,
    'accept'    => 'image/*,.pdf',
    'maxSize'   => 10,                     // MB
    'hint'      => '将文件拖到此处，或点击选择',
    'value'     => [],                     // 已上传文件 [{name,url,size}]
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::dropzoneUpload([
    'id' => 'xf-dropzone',
    'url' => '',
    'multiple' => true,
    'accept' => '*',
    'maxSize' => 10,    // MB
    'hint' => '将文件拖到此处，或点击浏览',
    'value' => [],
    'name' => 'file',
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `value[]`：已上传文件的地址列表（用于回显）

> **渲染骨架**：主要 class `xf-dropzone` `border` `rounded-3` `p-4` `text-center` `xf-dropzone-list` `d-flex` `flex-wrap`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `id` | string | `'xf-dropzone'` | 根元素 id（留空自动生成唯一 id）；**文本槽位**：输出前自动 HTML 转义 |
| `url` | string | `''` | 链接地址（自动做安全协议校验）；URL：经协议白名单校验（拦截 `javascript:` 等） |
| `multiple` | bool | `true` | 是否多选；源码用法：`. ' data-multiple="' . ($this->get('multiple') ? '1' : '0') . '"'` |
| `accept` | string | `'*'` | **文本槽位**：输出前自动 HTML 转义 |
| `maxSize` | int | `10` | MB |
| `hint` | string | `'将文件拖到此处，或点击浏览'` | 输入框下方提示文本；**文本槽位**：输出前自动 HTML 转义 |
| `value` | array | `[]` | 当前值（表单控件值 / 展示数值）；源码用法：`$initial = json_encode((array) $this->get('value'), JSON_HEX_TAG \| JSON_HEX_AMP);` |
| `name` | string | `'file'` | 表单字段名 / 语义名称；**文本槽位**：输出前自动 HTML 转义 |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `backToTop`

回到顶部悬浮按钮。

> **类**：`zxf\XfAdmin\Components\UI\BackToTop`
> **文件**：`src/Components/UI/BackToTop.php`（32 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::backToTop([
    'offset' => 300,        // 滚动多少像素后显示
    'icon'   => 'ti ti-arrow-up',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::backToTop([
    'offset' => 300,
    'icon' => 'ti ti-arrow-up',
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `offset` | int | `300` | 栅格偏移；源码用法：`$cfg = ['offset' => (int) $this->get('offset')];` |
| `icon` | string | `'ti ti-arrow-up'` | Tabler 图标 class，如 `ti ti-user`；源码用法：`$icon = (string) $this->get('icon');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `callout`

强调提示块（左侧色条，variant 语义）。

> **类**：`zxf\XfAdmin\Components\UI\Callout`
> **文件**：`src/Components/UI/Callout.php`（45 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::callout([
    'title'   => '提示',
    'body'    => '这是一段说明文字。',
    'variant' => 'info',   // info | success | warning | danger | primary
    'icon'    => 'ti ti-info-circle',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::callout([
    'title' => '',
    'body' => '',
    'variant' => 'info',
    'icon' => '',
]);
```

</details>

> **渲染骨架**：主要 class `xf-callout-body` `xf-callout-title` `xf-callout-text`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题）；源码用法：`$title = (string) $this->get('title');` |
| `body` | string | `''` | 主体内容（可为 HTML 字符串、组件实例或数组，原样输出）；源码用法：`$body = (string) $this->get('body');` |
| `variant` | string | `'info'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`$v = (string) $this->get('variant');` |
| `icon` | string | `''` | Tabler 图标 class，如 `ti ti-user`；源码用法：`$icon = (string) $this->get('icon');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `countdown`

倒计时（数字翻牌到零）。

> **类**：`zxf\XfAdmin\Components\UI\Countdown`
> **文件**：`src/Components/UI/Countdown.php`（57 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::countdown([
    'target'  => '+1 day',          // 目标时间：时间字符串 / 时间戳(秒) / DateTime 对象
    'title'   => '距活动结束',
    'labels'  => ['天', '时', '分', '秒'],
    'expired' => '已结束',
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::countdown([
    'target' => '',
    'title' => '',
    // labels
    'labels' => [
        '天',
        '时',
        '分',
        '秒',
    ],
    'expired' => '已结束',
]);
```

</details>

**数据结构**（数组元素可用键，由源码 `foreach` 解析）

- `labels[]`：四个单位文案，顺序为 `['天','时','分','秒']`

> **渲染骨架**：主要 class `xf-countdown-title` `small` `text-muted` `mb-1` `xf-countdown-blocks` `d-inline-flex` `align-items-center` `gap-2`

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `target` | string | `''` | 目标（组件语义不同：链接打开方式 `_blank` / 倒计时目标时间 / 数值目标）；源码用法：`$target = $this->get('target');` |
| `title` | string | `''` | 标题文本（部分组件为弹窗/tooltip 标题）；源码用法：`$title = (string) $this->get('title');` |
| `labels` | array | `['天', '时', '分', '秒']` | 标签或文案数组（组件语义不同：按钮文案 / 单位文案 / 图表标签） |
| `expired` | string | `'已结束'` | 源码用法：`$cfg = ['target' => $target, 'labels' => $labels, 'expired' => (string) $this->get('e…` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `countUp`

数字滚动递增动画。

> **类**：`zxf\XfAdmin\Components\UI\CountUp`
> **文件**：`src/Components/UI/CountUp.php`（40 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::countUp([
    'value'    => 1280,
    'prefix'   => '',
    'suffix'   => '',
    'decimals' => 0,
    'duration' => 1500,        // 动画时长（毫秒）
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::countUp([
    'value' => 0,
    'prefix' => '',
    'suffix' => '',
    'decimals' => 0,
    'duration' => 1500,
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `value` | int | `0` | 当前值（表单控件值 / 展示数值）；源码用法：`'value' => (float) $this->get('value'),` |
| `prefix` | string | `''` | 源码用法：`'prefix' => (string) $this->get('prefix'),` |
| `suffix` | string | `''` | 后缀文本；源码用法：`'suffix' => (string) $this->get('suffix'),` |
| `decimals` | int | `0` | 源码用法：`'decimals' => (int) $this->get('decimals'),` |
| `duration` | int | `1500` | 动画时长（毫秒）；源码用法：`'duration' => (int) $this->get('duration'),` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

---

### `invoicePrintButton`

发票打印 / 下载按钮 触发 window.print()（配合打印样式仅打印发票区域），复刻 inspinia apps-invoice 的打印按钮。 'text'    => '打印 / 下载 PDF', 'variant' => 'primary', 'target'  => '#invoice-print-area',  // 可选：仅打印该区域 ])。

> **类**：`zxf\XfAdmin\Components\UI\InvoicePrintButton`
> **文件**：`src/Components/UI/InvoicePrintButton.php`（40 行）
> **依赖插件**：无

**用法示例**

```php
XfAdmin::invoicePrintButton([
    'text'    => '打印 / 下载 PDF',
    'variant' => 'primary',
    'target'  => '#invoice-print-area',  // 可选：仅打印该区域
]);
```

<details><summary><b>全参数示例</b>（点击展开：列出该组件全部可配置参数，值均为默认值）</summary>

```php
echo XfAdmin::invoicePrintButton([
    'text' => '打印 / 下载 PDF',
    'variant' => 'primary',
    'target' => null,
    'icon' => 'ti ti-printer',
]);
```

</details>

**配置参数**

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `text` | string | `'打印 / 下载 PDF'` | 正文/按钮文案（纯文本语义，输出时转义）；源码用法：`$text = $this->get('text');` |
| `variant` | string | `'primary'` | 语义变体：primary/secondary/success/danger/warning/info/light/dark（受白名单约束）；源码用法：`$variant = $this->enum($this->get('variant'), array_merge(self::ENUM_VARIANT, self::E…` |
| `target` | mixed | `null` | 目标（组件语义不同：链接打开方式 `_blank` / 倒计时目标时间 / 数值目标）；源码用法：`$target = $this->get('target');` |
| `icon` | string | `'ti ti-printer'` | Tabler 图标 class，如 `ti ti-user`；源码用法：`$icon = $this->get('icon');` |

> 说明：`defaults()` 中以数组 `+` 合并的公共字段（如表单类的 `name`/`label`/`value`）见各组件所属基类说明；「内容槽位」原样输出 HTML，「文本槽位」自动转义。

